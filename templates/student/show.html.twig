{% extends 'base.html.twig' %}

{% block title %}Étudiant -
	{{ student.name }}
{% endblock %}

{% block body %}
	<div class="row">
		<div class="col-12 d-flex flex-column align-items-center">
		<pre>
			{{ dump(classrooms[0].lessons[0]) }}
		</pre>
		</div>
	</div>
{% endblock %}
