{% extends 'base.html.twig' %}

{% block title %}Create Question{% endblock %}

{% block body %}
    {% for message in app.flashes('success') %}
        <div class="alert alert-success mr-2 mb-0">
            {{ message }}
        </div>
    {% endfor %}

    <div class="d-flex justify-content-between">
        <h1>Créer Question</h1>
        <div class="text-right">
            <a class="btn btn-primary" href="{{ path('teacher_index') }}"
               role="button">{{ button|default('Finir questionnaire') }}</a>
        </div>
    </div>

    {{ include('question/_form.html.twig') }}

{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}